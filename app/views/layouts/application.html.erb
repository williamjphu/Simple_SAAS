<!DOCTYPE html>
<html>
  <head>
    <title>MyPageSAAS</title>
    
    <%= stylesheet_link_tag    'application', media: 'all' %> <!--'data-turbolinks-track': 'reload' %> -->
    <%= javascript_include_tag 'https://js.stripe.com/v2/', type: 'text/javascript' %>
    <%= javascript_include_tag 'application' %> <!-- 'data-turbolinks-track': 'reload' %>
  </head> -->
    <%= tag :meta, :name => "stripe-key", :content => STRIPE_PUBLIC_KEY %>
    <%= csrf_meta_tags %>

  <body>

  	<!-- Navigation bar for the app -->
  	<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  		<div class='container'>
  			<div class="navbar-header">
  				<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav-collapse">
  					<span class="sr-only">Togglen navigation</span>
  					<span class="icon-bar"></span>
  					<span class="icon-bar"></span>
  					<span class="icon-bar"></span>
  				</button>
  				 <!-- Rails syntax code below -->
  				<%= link_to root_path, class: 'navbar-brand' do %>
  					<i class="fa fa-user-circle-o"></i>
  					MyPageSAAS <!-- Title of the page -->
  				<% end %>

  			</div>

  			<div class="collapse navbar-collapse" id="main-nav-collapse">
  				<div class="nav navbar-nav navbar-right">
  					<!-- If logged in, following is true (Not '=' means to hide from HMTL file while inspecting-->
  					<% if current_user %>
  						<%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-warning navbar-btn" %>
  						<!-- %= button_to "Update profile", edit_user_registration_path, method: :get, class: "btn btn-info navbar-btn" % -->
  					<% else %>
  						<%= link_to "Log In", new_user_session_path, class: "btn btn-primary navbar-btn", method: :get%>
  					<% end %>
  				</div>
  				<ul class="nav navbar-nav navbar-right">
  					<!-- Ruby code below -->
  					<!-- "/about" or abou_path -->
  					<li><%= link_to "About", about_path %></li>
  					<li><%= link_to "Contact", new_contact_path %></li>
  					<li><%= link_to "FAQ", "#" %></li>
  				</ul>
  			</div>
  		</div>
  	</nav>

  	<div class="container">
  		<% flash.each do |key, value| %>
  			<%= content_tag :div, value, class: "alert alert-#{key}" %>
  		<% end %>
    	
    	<%= yield %>
	</div>

  </body>
</html>
